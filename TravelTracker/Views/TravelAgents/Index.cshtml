@model IEnumerable<TravelTracker.Domain.TravelAgent>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.AgentName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Role)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AgentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TravelAgentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TravelAgentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TravelAgentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
